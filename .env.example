# Postgres connection string from Neon / Vercel Postgres / Supabase etc.
# For Vercel Postgres / Neon copy the "DATABASE_URL" (with pooling) value
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
